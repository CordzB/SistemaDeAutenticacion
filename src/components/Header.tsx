import React from 'react';

const Header: React.FC = () => {
  return (
    <header className="bg-primary text-white py-3 w-100 text-center">
      <h1>Sistema de Autenticación</h1>
    </header>
  );
};

export default Header;
